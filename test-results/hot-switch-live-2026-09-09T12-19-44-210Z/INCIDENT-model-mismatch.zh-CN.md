# 运行中模型与设置不一致

2026-09-09 北京时间，第二轮采集仍在运行。用户报告已选择 Terra，但界面出现 Astra 元数据警告。

## 已确认的请求事实

- 20:19:44 采集基线：供应商二，模型偏好 gpt-6-astra。
- 20:21:07.467，fetch-5：向 https://wawapii.com/v1/responses 请求 gpt-6-astra，HTTP 200。
- 20:21:23.070，持久化偏好更新为 gpt-5.6-terra。
- 20:21:27.597、20:21:55.600、20:22:11.267，fetch-7/11/13：仍向供应商二请求 gpt-6-astra，均 HTTP 200。
- 20:22:40.934 收到供应商一同步深链。
- 20:22:43.179 采样确认新地址 https://hk.ai.soulecho.cc/v1、新 Key，偏好仍为 gpt-5.6-terra；20:22:44.007 发出 connected。
- 20:22:45.478，fetch-25：仍向旧供应商二使用旧 Key 请求 gpt-6-astra，HTTP 401。该事实不单独证明旧 Key 的具体撤销机制。
- 20:22:55.431 起，fetch-29 及后续：使用供应商一的新地址、新 Key，但模型仍是 gpt-6-astra，连续 HTTP 503。
- 供应商一实际返回的六个模型不包含 gpt-6-astra，包含 gpt-5.6-terra。截至 20:23:39，捕获 15 个这类 503 请求。503 的具体服务端原因仍需错误详情或上游记录，不能只凭列表缺失断言。

检查时当前会话的托管配置 `.modmind/external-agents/codex-homes/21a3230e03772a58aff1/quota/config.toml` 中 model 已为 gpt-5.6-terra，实际 POST 请求仍使用 Astra。说明不能用偏好文件或托管配置文件的内容代替运行时请求证据。尚未锁定模型固定在原生会话、运行参数还是其他缓存层。

## 警告含义与边界

事件 74、76（20:21:07）及 105、107（20:21:27）记录：`Model metadata for gpt-6-astra not found. Defaulting to fallback metadata; this can degrade performance and cause issues.`

该警告说明运行时未找到对应模型元数据而采用兜底信息，不等价于上游拒绝调用。供应商二最初这些请求为 HTTP 200；切供应商一后的 503 是另一个实际请求失败证据。HTTP 200 本身也不保证整个流式任务最终成功。

模型名为客户端请求字段；没有上游内部记录，不能证明后端实际模型身份。

## 下一步

保留现场，不修改任务或设置。继续记录用户计划的免费线路切换，观察地址、Key、模型是否一致更新，以及是否仍使用 Astra 重试。完成后按任务和原生会话编号进一步区分自动恢复与新任务启动，避免误判为同一个运行实例的无缝接管。
